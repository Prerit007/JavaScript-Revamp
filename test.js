"use strict"; // treat all JavaScript codes as newer version of JavaScript.

const accountId = 12; //number
let accountType = 'savings' //string
var accountEmail = "prer@gmail.com"
accountNo = 1331343434
let isLoggedIn = false //boolean
/*
We can use console.table() to display the data in a table format.
We dont usually use var for declaring variables. Since it has issues related to scope. (Block/Functional)
*/
console.table([accountId, accountType, accountEmail, accountNo]);


